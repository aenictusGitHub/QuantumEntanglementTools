# Julia-native API

This page contains the exported Julia-native bindings. Certificate, tolerance,
ordering, and sparse-storage contracts are defined in each docstring and in the
linked conceptual guides.

```@autodocs
Modules = [QuantumEntanglementTools]
Private = false
```
