<!-- Generated by scripts/build_qetlab_completion_plan.jl; do not edit by hand. -->
# QETLAB completion plan

This plan is generated from the reviewed status overlay, the generated QETLAB inventory, public provenance, and the small capability policy in `porting/qetlab_completion_policy.toml`.

## Current snapshot

| Measure | Rows |
|---|---:|
| Public inventory | 127 |
| Implementation-complete | 127 |
| Partial | 0 |
| Deferred | 0 |
| Blocked with an explicit reason | 0 |
| Incomplete public queue | 0 |
| Internal helpers | 36 |

- QETLAB revision: `d8589610f00cff106537268dee2e2a1153f3a601`
- Inventory SHA-256: `62d0e94cf73934f4b35af3364b81cdf7468ab1662705e8171d7e46c57ec6a3c5`
- Status-overlay SHA-256: `40eae81340d3241aa12d844794ee67417a97b5aa84a18567c3c090b7551edd4d`
- Provenance SHA-256: `5b9194294716baeaa0a0e42e702957569433809e1b703debbafaab2f064993e6`
- Completion-policy SHA-256: `9d71c90a7001085b331383239c30d1c42689ebd56da2b9369fd312358a26b1c7`

The all-public-row machine ledger is [`porting/qetlab_completion_plan.toml`](../porting/qetlab_completion_plan.toml). The executable queue is [`porting/qetlab_completion_queue.toml`](../porting/qetlab_completion_queue.toml).

## Ordering semantics

The queue is ordered by reviewed capability package, then by rank in the static upstream source-call graph, then by function name. The 503-edge graph is evidence about calls in the pinned MATLAB source. It is not an implementation DAG: it cannot express package-owned architecture, and a call to a partially implemented row may use a branch that is already available. Capability prerequisites therefore control package order; the source-call dependencies remain visible on every task for review.

## 1. General rectangular and two-sided map model

Complete the package-owned operator-space and two-sided operator-sum model before extending channel wrappers or optimization routines.

**Requires:** `reviewed_source_inventory`, `sparse_and_densification_policy`, `typed_square_map_representations`

**Provides:** `general_rectangular_operator_space_model`, `hermiticity_preserving_map_diagnostics`, `two_sided_operator_sum_model`

This is an architecture gate. It intentionally owns no upstream row; later solver-backed packages depend on its result contracts.

## 2. Solver-free spectral and norm foundations

Finish exact or structured solver-free branches, separating exact values from bounds and unsupported symbolic optimizer expressions.

**Requires:** `reviewed_source_inventory`, `sparse_and_densification_policy`, `structured_numeric_predicates`, `validated_spectral_primitives`

**Provides:** `full_spectral_measure_contracts`, `operator_schmidt_completion`, `structured_norm_bound_contracts`

This is an architecture gate. It intentionally owns no upstream row; later solver-backed packages depend on its result contracts.

## 3. Bounded algebraic and iterative foundations

Provide bounded convergence, explicit allocation policy, state restoration, and structured failure evidence for iterative algebra and map construction.

**Requires:** `checked_dimension_and_combinatorial_guards`, `explicit_rng_contract`, `general_rectangular_operator_space_model`, `operator_schmidt_completion`

**Provides:** `bounded_iterative_linear_algebra`, `bounded_multidimensional_iteration`, `random_map_construction_foundations`

This work package is complete; no public row or required private helper remains queued.

## 4. States, subspaces, UPBs, and exact entanglement constructions

Complete guarded state and subspace constructions with explicit RNG, theorem domains, witnesses, and family-level provenance.

**Requires:** `checked_dimension_and_combinatorial_guards`, `explicit_rng_contract`, `operator_schmidt_completion`, `structured_certificate_and_unknown_semantics`, `tested_product_analysis`

**Provides:** `verified_state_and_subspace_constructions`, `verified_upb_construction_and_analysis`

This work package is complete; no public row or required private helper remains queued.

## 5. Typed polynomial and copositivity foundations

Define type-preserving polynomial data, coefficient ordering, deterministic work budgets, and corrected solver-free bound semantics.

**Requires:** `checked_dimension_and_combinatorial_guards`, `explicit_rng_contract`, `reviewed_source_inventory`, `sparse_and_densification_policy`

**Provides:** `bounded_polynomial_algebra`, `typed_polynomial_representation`

This is an architecture gate. It intentionally owns no upstream row; later solver-backed packages depend on its result contracts.

## 6. Solver-independent optimization architecture

Define package-owned models and result contracts, optimizer extension boundaries, residual checks, bounds, statuses, and certificate retention before solver-backed rows.

**Requires:** `full_spectral_measure_contracts`, `general_rectangular_operator_space_model`, `structured_certificate_and_unknown_semantics`, `structured_norm_bound_contracts`, `typed_polynomial_representation`

**Provides:** `optional_solver_extension_contract`, `solver_independent_optimization_models`, `status_aware_optimization_results`

This is an architecture gate. It intentionally owns no upstream row; later solver-backed packages depend on its result contracts.

## 7. Channel norms and distinguishability optimization

Implement channel SDPs and analytic shortcuts over the completed general-map model with validated solver status and primal-dual evidence.

**Requires:** `general_rectangular_operator_space_model`, `optional_solver_extension_contract`, `solver_independent_optimization_models`, `status_aware_optimization_results`, `two_sided_operator_sum_model`

**Provides:** `status_aware_channel_optimization`

This work package is complete; no public row or required private helper remains queued.

## 8. Entanglement and state optimization

Complete absolute-PPT, symmetric-extension, distinguishability, randomized-PPT, UPB, and composite separability strategies without Boolean collapse.

**Requires:** `bounded_iterative_linear_algebra`, `general_rectangular_operator_space_model`, `optional_solver_extension_contract`, `status_aware_optimization_results`, `structured_certificate_and_unknown_semantics`, `verified_state_and_subspace_constructions`, `verified_upb_construction_and_analysis`

**Provides:** `status_aware_entanglement_optimization`, `status_aware_state_discrimination`

This work package is complete; no public row or required private helper remains queued.

## 9. S(k) norms, dual norms, and block positivity

Separate exact cases, bounds, randomized evidence, solver relaxations, witnesses, and unknown outcomes under explicit budgets.

**Requires:** `explicit_rng_contract`, `general_rectangular_operator_space_model`, `optional_solver_extension_contract`, `status_aware_optimization_results`, `structured_norm_bound_contracts`

**Provides:** `status_aware_sk_norm_bounds`, `structured_block_positivity_results`

This work package is complete; no public row or required private helper remains queued.

## 10. Coherence criteria and robustness optimization

Correct recorded theorem/helper defects, implement analytic branches, and expose optimizer-backed results with closest-state or noise evidence.

**Requires:** `full_spectral_measure_contracts`, `general_rectangular_operator_space_model`, `optional_solver_extension_contract`, `status_aware_optimization_results`

**Provides:** `status_aware_coherence_optimization`

This work package is complete; no public row or required private helper remains queued.

## 11. Polynomial SOS and copositivity solver models

Build sparse status-aware SOS and copositivity models on the typed polynomial layer, preserving lower and upper bound semantics.

**Requires:** `bounded_polynomial_algebra`, `optional_solver_extension_contract`, `status_aware_optimization_results`, `typed_polynomial_representation`

**Provides:** `status_aware_polynomial_optimization`

This work package is complete; no public row or required private helper remains queued.

## 12. Bell scenarios, nonlocal games, and hierarchies

Introduce typed Bell-scenario data and guarded exact, heuristic, and hierarchy routes with explicit RNG, solver status, and bound evidence.

**Requires:** `bounded_multidimensional_iteration`, `checked_dimension_and_combinatorial_guards`, `explicit_rng_contract`, `optional_solver_extension_contract`, `status_aware_optimization_results`

**Provides:** `status_aware_nonlocal_game_optimization`, `typed_bell_scenario_representation`

This work package is complete; no public row or required private helper remains queued.

## Internal-helper dispositions

The 36 private helpers are not public parity promises. All 36 have terminal superseded/excluded dispositions; none remains required by an incomplete public capability. Direct and transitive source-call consumers are recorded where detected.

- Terminal: `CG2FC`, `CG2FP`, `FC2CG`, `FC2FP`, `FP2CG`, `FP2FC`, `asum_vector`, `asymind`, `bcs_to_nonlocal`, `chshd`, `dec_to_bin`, `exp2ind`, `ffl`, `glob_ind`, `has_band_k_ordering`, `iden`, `jacobi_poly`, `normalize_cols`, `one_factorization`, `opt_args`, `opt_disp`, `pad_array`, `perfect_matchings`, `perm_inv`, `perm_sign`, `poly_rand_input`, `pure_to_mixed`, `sk_iterate`, `spnull`, `sporth`, `sum_vector`, `superoperator_dims`, `symind`, `symindfind`, `update_odometer`, `vec_partitions`
- Required: —

## Checker scope and limitations


- `julia --startup-file=no --project=. scripts/check_qetlab_completion.jl` validates the completed static ledger and its repository evidence. That result is an implementation-completeness claim, not a MATLAB-parity or release approval claim.
- `julia --startup-file=no --project=. scripts/check_qetlab_completion.jl --strict` is an implementation-completeness gate and passes for the generated ledger: all 127 public rows and all 36 private-helper dispositions are terminal.
- The static evidence gate checks runtime exports against unique provenance entries, referenced implementation/specification/test/doc files, support for implementation-complete dispositions, duplicate public definitions, compatibility delegation, placeholder markers, library RNG calls, and explicit-RNG tests for completed randomized rows.
- Every implementation-complete solver-backed row must have a matching package extension, a dedicated test environment with its own `Project.toml`, documented architecture, and status plus failure/inconclusive test evidence.
- `julia --startup-file=no --project=. test/qetlab_completion_checker.jl` runs focused mutation fixtures proving that missing files/provenance, RNG evidence, solver-extension evidence, duplicate definitions, placeholders, and global-RNG calls are rejected.
- This strict pass does not by itself establish MATLAB parity, semantic correctness, solver certificate validity, supported-platform coverage, or the maintainer's non-delegable release review.
- `required_backend` preserves upstream toolbox facts from the inventory; it does not select or endorse a Julia optimizer.
- `known_upstream_defects` carries the inventory's recorded numerical or semantic ambiguity strings. An empty list is not proof that upstream has no defect.
- Binding and file lists come only from direct QETLAB provenance rows. An empty list can coexist with documented project-native component evidence and is not filled by name guessing.
- Per-row benchmark files point to the shared benchmark driver only when the inventory says a benchmark was recorded; the present provenance schema has no case-level benchmark mapping.
